
function setup() {
  createCanvas(400, 400);
  colorMode(HSB, width, height, 100);
}

function draw() {
  var x = random(width);
  var y = random(height);
	
	for (var x; x < 100; x = x + 100) {
    line(x, height/2, x+100, height/2-75);
  }

  stroke(x, y, 100);
  point(x, y);
}
